<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nikhil.noduesapp_v1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/splash_screen_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".Splashscreen_layout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Add_Due" />
        <activity android:name=".Check_Due" />
        <activity android:name=".Form_Due" />
        <activity android:name=".Remark_NoDue" />
        <activity android:name=".Disp_Form_Due" />
        <activity android:name=".nodues_show_all" />
        <activity android:name=".Register"></activity>
    </application>

</manifest>